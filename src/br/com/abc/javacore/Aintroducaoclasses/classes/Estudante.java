package br.com.abc.javacore.Aintroducaoclasses.classes;

/**
 * Estudante
 */
public class Estudante {
    //Classes são moldes, objetos são as coisas criadas a partir dos moldes
    //Isso se chama classe de domínio. Aqui, você não cria métdo main, mas apenas mapeia algo do mundo real
    // As classes devem ser coesas, ou seja, apenas representar a contraparte do mundo real, não executar o programa
    // Exemplo de baixa coesão (que é algo ruim): mercado
    //Toda classe tem atributo (variaveis) e comportamento (métodos)
    //A classe é uma abstração (modelo) do mundo real e só contém o que importa para o sistema, não todas as 
    // caraterísticas da contraparte real
    public String nome;
    public String matricula;
    public int idade;

    
}